{
  "$schema": "https://schema.railway.app/config/v1.json",
  "build": {
    "buildCommand": "cd frontend && npm install && npm run build && pip install -r server_python/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd server_python && uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
